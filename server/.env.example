PORT=5000

# SMTP settings for the contact form email notifications.
# Leave blank during development — messages still save to the database without these.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO=
